<div class="col-12">

<?php if (empty($orders)): ?>

    <!-- EMPTY ORDER -->
    <div class="card border-0 shadow-sm rounded-4 text-center p-5 bg-white">

        <div class="mb-3 fs-1">🛍️</div>

        <h4 class="fw-bold mb-2">Chưa có đơn hàng nào</h4>

        <p class="text-muted mb-4">
            Bạn chưa mua sản phẩm nào. Khám phá cửa hàng ngay hôm nay.
        </p>

        <div>
            <a href="<?= BASE_URL ?>?action=product/index"
               class="btn btn-primary px-4 py-2 rounded-3 fw-semibold">
                Mua sắm ngay
            </a>
        </div>

    </div>

<?php else: ?>

    <!-- ORDER LIST -->
    <div class="card border-0 shadow-lg rounded-4 overflow-hidden">

        <!-- HEADER -->
        <div class="bg-primary text-white px-4 py-3">
            <h5 class="mb-0 fw-bold">📦 Đơn hàng của bạn</h5>
        </div>

        <!-- TABLE -->
        <div class="table-responsive">

            <table class="table table-hover align-middle mb-0">

                <thead class="table-light">
                    <tr>
                        <th class="ps-4">Mã đơn</th>
                        <th>Tổng tiền</th>
                        <th>Trạng thái</th>
                        <th>Ngày đặt</th>
                        <th class="text-center pe-4" style="width:140px;">
                            Thao tác
                        </th>
                    </tr>
                </thead>

                <tbody>

                <?php foreach ($orders as $order): ?>

                    <tr>

                        <!-- ID -->
                        <td class="ps-4 fw-semibold text-primary">
                            #<?= (int)$order['id'] ?>
                        </td>

                        <!-- TOTAL -->
                        <td class="fw-bold text-danger">
                            <?= number_format(order_total_amount($order)) ?> đ
                        </td>

                        <!-- STATUS -->
                        <td>
                            <span class="badge rounded-pill px-3 py-2 bg-light text-dark border">
                                <?= htmlspecialchars(order_status_label($order['status'])) ?>
                            </span>
                        </td>

                        <!-- DATE -->
                        <td class="text-muted small">
                            <?= htmlspecialchars($order['created_at']) ?>
                        </td>

                        <!-- ACTION -->
                        <td class="text-center pe-4">
                            <a href="<?= BASE_URL ?>?action=order/detail&id=<?= (int)$order['id'] ?>"
                               class="btn btn-sm btn-outline-primary rounded-3 px-3">
                                Chi tiết
                            </a>
                        </td>

                    </tr>

                <?php endforeach; ?>

                </tbody>

            </table>

        </div>

    </div>

<?php endif; ?>

</div>
